# Copy to confer-agent.profile.yml inside a project repo and fill in real values.
# This file is per-project. Templates reference it.

env: dev              # dev | staging | prod
http_port: 3000       # Backend API port (set based on your infra)
frameworks: ["next@14", "fastapi@0.115"]
db: "postgres+drizzle"
auth: "clerk"
infra: "coolify|vercel|replit"  # Choose applicable infrastructure
notes: "This file is per-project. Templates reference it."

